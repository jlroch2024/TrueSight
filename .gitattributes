# Keep the Maven wrapper and shell scripts with Unix line endings, so they run on the Linux machines GitHub
# Actions and Google Cloud use. Without this, a Windows checkout can break them.
mvnw text eol=lf
*.sh text eol=lf
mvnw.cmd text eol=crlf
